extends layout

block content
    .container
        form(action="/deleteUser")
            input(type='text', name='username', id='inputEmail', placeholder='Username', required, autofocus)
            input(type='submit')
            | DELETE
        h3
            li
                a(href = "/adminMenu" style="color: white") Menu