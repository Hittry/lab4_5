extends layout

block content
    .container
        form.form-signin(role='form', action="/reg", method="post")
            div.text
                h2.form-signin-heading Sign Up here
            input.form-control(type='text', name="username", placeholder='Your Name')
            input.form-control(type='password', name="password", placeholder='Your Password')
            button.btn.btn-lg.btn-primary(type='submit') Sign Up